<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme._2340CollegeScheduler"
        tools:targetApi="31">

        <!-- Make WeekAtAGlanceActivity the first activity to launch -->
        <activity android:name=".WeekAtAGlanceActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Declare AddClass activity without it being the launcher -->
        <activity android:name=".AddClass"
            android:exported="true" />

        <activity android:name=".AddAssignmentActivity"
            android:label="Add Assignment">
        </activity>


    </application>

</manifest>
